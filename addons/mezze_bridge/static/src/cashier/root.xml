<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="mezze_bridge.Root">
        <div class="mz-app" t-att-data-phase="state.phase">

            <!-- WORKSPACE RAIL — the prototype's information architecture: ONE page, with
                 every workspace reachable from a persistent 68px rail. It lives at the app
                 root (not inside the menu phase) so navigation exists in EVERY phase by
                 construction, which is what the payment-screen defect was really about.
                 Destinations that are separate devices in the pilot (Floor, Kitchen) keep
                 their standalone routes AND open here as modals. -->
            <WorkspaceRail active="railActiveKey" configId="boot.config_id or null"
                           mark="railMark" userName="userName"
                           onSelect="(k) => this.onRailSelect(k)"/>

            <div class="mz-shell">

            <!-- Top bar -->
            <header class="mz-topbar">
                <div class="mz-brand">
                    <span class="mz-logo">Mezze</span>
                    <span class="mz-branch" t-esc="branchName"/>
                </div>
                <!-- R2A: one-product Floor ↔ Register ↔ Orders ↔ Reservations nav -->
                <nav class="mz-nav" aria-label="Workspaces">
                    <a class="mz-nav__item" t-att-href="floorUrl">Floor</a>
                    <t t-set="onOrders" t-value="state.phase === 'orders' || state.phase === 'completed'"/>
                    <t t-set="onHost" t-value="state.phase === 'reservations'"/>
                    <button class="mz-nav__item" type="button"
                            t-att-class="{ 'mz-nav__item--active': !onOrders and !onHost }"
                            t-att-aria-current="(!onOrders and !onHost) ? 'page' : false"
                            t-on-click="() => this.backToRegister()">Register</button>
                    <button class="mz-nav__item" type="button"
                            t-att-class="{ 'mz-nav__item--active': onOrders }"
                            t-att-aria-current="onOrders ? 'page' : false"
                            t-on-click="() => this.openOrders()">Orders</button>
                    <button class="mz-nav__item" type="button"
                            t-att-class="{ 'mz-nav__item--active': onHost }"
                            t-att-aria-current="onHost ? 'page' : false"
                            t-on-click="() => this.openHost()">Reservations</button>
                </nav>
                <!-- R2A CP5/6: table-bound context + authoritative guest count. Obvious
                     but quieter than Total/Charge; 44px steppers, tabular numeral. -->
                <!-- (the table + guest context moved into the order panel head, where the
                     prototype puts it — it must not appear twice) -->
                <!-- PROTOTYPE TOPBAR: a session pill beside the brand. The prototype also
                     shows a role chip, an EN/ع toggle and a help button; this product has
                     no role in the boot payload, no language-switch endpoint and no help
                     system, so those are absent rather than decorative. -->
                <span t-if="sessionLabel" class="mz-sesspill" t-att-title="sessionLabel">
                    <span class="mz-sesspill__dot" aria-hidden="true"/>
                    <span class="mz-sesspill__t" t-esc="sessionLabel"/>
                </span>
                <div class="mz-topbar-right">
                    <span class="mz-conn">
                        <span t-attf-class="mz-status mz-status--sm mz-status--{{connVariant}}"
                              t-att-data-state="state.conn.local" t-att-title="connLabel">
                            <span class="mz-status__dot"/><t t-esc="connLabel"/>
                        </span>
                        <span t-attf-class="mz-status mz-status--sm mz-status--{{wanVariant}}"
                              t-att-data-state="state.conn.wan" t-att-title="wanLabel">
                            <span class="mz-status__dot"/><t t-esc="wanLabel"/>
                        </span>
                    </span>
                    <span class="mz-user" t-esc="userName"/>
                    <!-- Drives the appearance contract the page bootstrap already owns
                         (?mzmode= > localStorage > prefers-color-scheme) — one theming
                         mechanism, not a second one. -->
                    <button type="button" class="mz-icon-btn mz-themetog"
                            t-att-aria-label="themeToggleLabel" t-att-title="themeToggleLabel"
                            t-att-aria-pressed="state.mzMode === 'dark' ? 'true' : 'false'"
                            t-on-click="() => this.toggleTheme()">
                        <span aria-hidden="true" t-esc="state.mzMode === 'dark' ? '☾' : '☀'"/>
                    </button>
                </div>
            </header>

            <!-- BOOTING -->
            <t t-if="state.phase === 'booting'">
                <div class="mz-state mz-state--info" role="status" aria-busy="true">
                    <div class="mz-spinner" aria-hidden="true"/>
                    <p>Loading menu…</p>
                </div>
            </t>

            <!-- AUTH REQUIRED -->
            <t t-elif="state.phase === 'auth_required'">
                <div class="mz-state mz-state--warn">
                    <h2>Authentication required</h2>
                    <p>Your session has expired. Please sign in again to continue.</p>
                    <a class="mz-btn mz-btn--primary" href="/web/login?redirect=/mezze/pos">Sign in</a>
                </div>
            </t>

            <!-- ERROR -->
            <t t-elif="state.phase === 'error'">
                <div class="mz-state mz-state--error">
                    <h2>Unable to continue</h2>
                    <p t-esc="state.errorMsg || 'Unable to load menu'"/>
                    <button class="mz-btn mz-btn--primary" t-on-click="() => this.retry()">Retry</button>
                </div>
            </t>

            <!-- MENU -->
            <t t-elif="state.phase === 'menu'">
                <!-- R2A CP5: stale / cross-branch table id — safe, explicit recovery
                     (no crash, no wrong-table fallback; Register still usable as counter). -->
                <div t-if="tableInvalid" class="mz-alert mz-alert--warning" role="status">
                    <span class="mz-alert__content">That table isn’t available on this branch.</span>
                    <a class="mz-btn mz-btn--sm mz-btn--secondary" t-att-href="floorUrl">Back to Floor</a>
                </div>
                <!-- CUSTOMER PICKER (menu phase). The picker markup used to live ONLY
                     inside the payment screen, so the order panel's "Add customer" set
                     the state and nothing rendered — the control looked dead. Attaching a
                     customer to the order belongs here; deposit / settle stay on the
                     payment screen, which is where money is handled. Same state, same
                     handlers, one source of truth. -->
                <div t-if="state.customerPicker and state.phase === 'menu'"
                     class="mz-modal-scrim" t-on-click="() => this.closeCustomerPicker()">
                    <div class="mz-modal mz-custpick" role="dialog" aria-modal="true"
                         t-att-aria-label="customerPickerTitle"
                         t-on-click="(ev) => ev.stopPropagation()">
                        <div class="mz-modal__head">
                            <h2 class="mz-modal__title" t-esc="customerPickerTitle"/>
                            <button class="mz-modal__x" t-att-aria-label="closeLabel"
                                    t-on-click="() => this.closeCustomerPicker()">✕</button>
                        </div>
                        <input class="mz-input mz-custpick__q" type="search"
                               t-att-placeholder="customerSearchLabel"
                               t-att-aria-label="customerSearchLabel" autocomplete="off"
                               data-testid="mz-customer-search"
                               t-att-value="state.customerPicker.query"
                               t-on-input="(ev) => this.searchCustomers(ev.target.value)"/>
                        <div t-if="state.customerPicker.error" class="mz-alert mz-alert--danger"
                             role="alert" t-esc="state.customerPicker.error"/>
                        <div class="mz-cust-results" data-testid="mz-customer-results">
                            <div t-if="state.customerPicker.busy" class="mz-cart-empty"
                                 t-esc="searchingLabel"/>
                            <div t-elif="!state.customerPicker.results.length" class="mz-cart-empty"
                                 t-esc="noCustomersLabel"/>
                            <button t-foreach="state.customerPicker.results" t-as="c" t-key="c.id"
                                    class="mz-cust-row"
                                    t-att-class="{'mz-cust-row--active': state.customer and state.customer.id === c.id}"
                                    t-att-data-cust-id="c.id"
                                    t-on-click="() => this.chooseCustomer(c)">
                                <span class="mz-cust-row-name" t-esc="c.name"/>
                                <span t-if="c.phone" class="mz-cust-row-phone" t-esc="c.phone"/>
                            </button>
                        </div>
                        <!-- CREATE. The till could only ever search, so a first-time guest
                             could not be attached to an order at all. Name is the only
                             required field — a walk-in should not need a form. -->
                        <div class="mz-custnew">
                            <button t-if="!state.customerPicker.creating" type="button"
                                    class="mz-btn mz-btn--secondary mz-custnew__open"
                                    t-on-click="() => this.startNewCustomer()"
                                    t-esc="newCustomerLabel"/>
                            <div t-else="" class="mz-custnew__form">
                                <input class="mz-input" type="text" t-att-placeholder="custNameLabel"
                                       t-att-aria-label="custNameLabel" autocomplete="off"
                                       data-testid="mz-customer-new-name"
                                       t-att-value="state.customerPicker.newName"
                                       t-on-input="(ev) => this.setNewCustomer('newName', ev.target.value)"/>
                                <input class="mz-input" type="tel" t-att-placeholder="custPhoneLabel"
                                       t-att-aria-label="custPhoneLabel" autocomplete="off"
                                       data-testid="mz-customer-new-phone"
                                       t-att-value="state.customerPicker.newPhone"
                                       t-on-input="(ev) => this.setNewCustomer('newPhone', ev.target.value)"/>
                                <div class="mz-custnew__actions">
                                    <button class="mz-btn mz-btn--secondary"
                                            t-on-click="() => this.cancelNewCustomer()" t-esc="cancelLabel"/>
                                    <button class="mz-btn mz-btn--primary"
                                            data-testid="mz-customer-new-save"
                                            t-att-disabled="(!state.customerPicker.newName.trim() or state.customerPicker.busy) ? 'disabled' : false"
                                            t-on-click="() => this.createCustomer()" t-esc="saveCustomerLabel"/>
                                </div>
                            </div>
                        </div>
                        <div class="mz-custpick__foot">
                            <button t-if="state.customer" class="mz-btn mz-btn--secondary"
                                    t-on-click="() => this.clearCustomer()" t-esc="clearCustomerLabel"/>
                            <button class="mz-btn mz-btn--primary"
                                    t-on-click="() => this.closeCustomerPicker()" t-esc="doneLabel"/>
                        </div>
                    </div>
                </div>

                <!-- RAIL VIEW SWITCH — the prototype swaps data-view in place; there is no
                     dialog. A destination that reads an endpoint takes over the workspace
                     area; Floor and Kitchen are their own pages and are navigated to. -->
                <div t-if="state.workspace" class="mz-workspace mz-workspace--ws">
                    <section class="mz-wsview">
                        <header class="mz-wsview__head">
                            <h2 class="mz-wsview__title" t-esc="workspaceTitle"/>
                            <button type="button" class="mz-btn mz-btn--sm mz-btn--secondary"
                                    t-on-click="() => this.closeWorkspace()" t-esc="backToRegisterLabel"/>
                        </header>
                        <SettingsPanel t-if="state.workspace === 'settings'" api="api"
                                       onChange.bind="onAppearanceChange"
                                       onElevate.bind="elevateSettings"/>
                        <Workspace t-else="" kind="state.workspace" api="api" currency="currency"/>
                    </section>
                </div>
                <div t-else="" class="mz-workspace">
                    <!-- (the workspace rail moved to the app root — see .mz-rail above) -->
                    <aside class="mz-catside" t-att-aria-label="categoriesLabel">
                        <div class="mz-catside__branch">
                            <span class="mz-catside__dot" aria-hidden="true"/>
                            <span class="mz-catside__bname" t-esc="branchName"/>
                        </div>
                        <p class="mz-catside__label" t-esc="categoriesLabel"/>
                        <div class="mz-catside__list">
                            <button t-if="hasFavorites" type="button" class="mz-catside__item"
                                    t-att-class="{ 'mz-catside__item--active': state.activeCategory === FAV }"
                                    t-att-aria-pressed="(state.activeCategory === FAV) ? 'true' : 'false'"
                                    t-on-click="() => this.onSelectCategory(FAV)">
                                <span class="mz-fav-star" aria-hidden="true">★</span>
                                <span class="mz-catside__n" t-esc="favLabel"/>
                                <span class="mz-catside__c" t-esc="favCount"/>
                            </button>
                            <button type="button" class="mz-catside__item"
                                    t-att-class="{ 'mz-catside__item--active': !state.activeCategory }"
                                    t-att-aria-pressed="(!state.activeCategory) ? 'true' : 'false'"
                                    t-on-click="() => this.onSelectCategory(null)">
                                <span class="mz-catside__n" t-esc="allItemsLabel"/>
                                <span class="mz-catside__c" t-esc="allCount"/>
                            </button>
                            <button t-foreach="state.categories" t-as="cat" t-key="cat.id"
                                    type="button" class="mz-catside__item"
                                    t-att-class="{ 'mz-catside__item--active': state.activeCategory === cat.id }"
                                    t-att-aria-pressed="(state.activeCategory === cat.id) ? 'true' : 'false'"
                                    t-on-click="() => this.onSelectCategory(cat.id)">
                                <span class="mz-catside__n mz-catside__data" t-esc="cat.name"/>
                                <span class="mz-catside__c" t-esc="categoryCount(cat.id)"/>
                            </button>
                        </div>
                    </aside>
                    <section class="mz-catalog">
                        <!-- R1B keyboard: fast product search. Press "/" to focus; type to
                             filter the whole catalog; ↑/↓ to move; Enter to add. -->
                        <div class="mz-searchbar">
                            <input t-ref="search" type="text" class="mz-search"
                                   placeholder="Search menu — press /"
                                   aria-label="Search menu"
                                   t-on-input="(ev) => this.onSearchInput(ev.target.value)"/>
                            <button t-if="isSearching" class="mz-search-clear"
                                    aria-label="Clear search" title="Clear (Esc)"
                                    t-on-click="() => this.clearSearch()">✕</button>
                            <span class="mz-kbd-hint" aria-hidden="true" t-esc="kbdHint"/>
                        </div>
                        <nav class="mz-catbar">
                            <button t-if="hasFavorites" class="mz-cat mz-cat--fav"
                                    t-att-class="{'mz-cat--active': state.activeCategory === FAV}"
                                    t-att-aria-pressed="(state.activeCategory === FAV) ? 'true' : 'false'"
                                    t-att-aria-label="favLabel"
                                    t-on-click="() => this.onSelectCategory(FAV)">
                                <span class="mz-fav-star" aria-hidden="true">★</span>
                                <span t-esc="favLabel"/>
                            </button>
                            <button class="mz-cat" t-att-class="{'mz-cat--active': !state.activeCategory}"
                                    t-att-aria-pressed="(!state.activeCategory) ? 'true' : 'false'"
                                    t-on-click="() => this.onSelectCategory(null)">All</button>
                            <button t-foreach="state.categories" t-as="cat" t-key="cat.id"
                                    class="mz-cat" t-att-class="{'mz-cat--active': state.activeCategory === cat.id}"
                                    t-att-aria-pressed="(state.activeCategory === cat.id) ? 'true' : 'false'"
                                    t-on-click="() => this.onSelectCategory(cat.id)"
                                    t-esc="cat.name"/>
                        </nav>
                        <!-- DESIGN FIDELITY: the reference states how many items the
                             current selection holds, directly above the grid. This is
                             the REAL length of what is rendered, so it doubles as a
                             live result count while searching. -->
                        <p class="mz-catalog__count" aria-live="polite" t-esc="availableLabel"/>
                        <ProductGrid products="filteredProducts" currency="currency"
                                     highlightId="highlightId"
                                     onSelect="(p) => this.onSelectProduct(p)"
                                     onEightySix="this.can('orders.write') ? ((p, avail) => this.toggleEightySix(p, avail)) : undefined"/>
                    </section>
                    <Cart onCharge="() => this.goToPayment()" inFlight="state.inFlight"
                          canSend="isTableBound" onSend="() => this.sendToTable()"
                          canAssign="canAssign" onAssign="() => this.openAssignPicker()"
                          canMove="canMove" onMove="() => this.openMovePicker()"
                          canPark="canPark" onPark="() => this.parkAndNew()"
                          tableLabel="isTableBound ? tableLabel : null"
                          tableSubLabel="tableSubLabel"
                          guests="guestsCount" onGuests="(d) => this.setGuests(d)"
                          customerName="customerName"
                          onCustomer="() => this.openCustomerPicker()"
                          onSeat="isTableBound ? (() => this.openSeatPicker()) : undefined"
                          onFire="this.offers('orders.fire') ? (() => this.fireOrder()) : undefined"
                          onVoid="this.offers('orders.void') ? (() => this.openVoidGate()) : undefined"
                          onComp="this.offers('orders.comp') ? ((line) => this.openCompGate(line)) : undefined"
                          orderTypes="orderTypes"
                          onOrderType="(k) => this.setOrderType(k)"
                          onSplit="() => this.goToPayment()"
                          onNote="(line) => this.openNote(line)"/>
                </div>
                <!-- Delivery: a different kind of order, so it asks for what a
                     delivery actually needs instead of relabelling the ticket. -->
                <div t-if="state.deliveryForm" class="mz-modal" role="dialog" aria-modal="true"
                     aria-label="Delivery">
                    <div class="mz-modal__panel mz-modal__panel--sm">
                        <DeliveryForm api="api" configId="boot.config_id"
                                      subtotal="order.estimatedTotal"
                                      currency="currency"
                                      customer="state.customer"
                                      onCreate.bind="createDelivery"
                                      onCancel.bind="closeDeliveryForm"/>
                    </div>
                </div>
                <!-- Kitchen note for one line. -->
                <div t-if="state.noteEdit" class="mz-modal" role="dialog" aria-modal="true"
                     t-att-aria-label="noteTitle">
                    <div class="mz-modal__panel mz-modal__panel--sm">
                        <div class="mz-note-edit">
                            <h3 class="mz-mgate__title" t-esc="noteTitle"/>
                            <p class="mz-mgate__detail" t-esc="state.noteEdit.name"/>
                            <input class="mz-input" type="text" maxlength="200"
                                   data-testid="mz-note-input" autofocus="autofocus"
                                   t-att-aria-label="noteTitle"
                                   t-att-value="state.noteEdit.text"
                                   t-on-input="(ev) => this.setNoteText(ev.target.value)"
                                   t-on-keydown="(ev) => ev.key === 'Enter' ? this.saveNote() : (ev.key === 'Escape' ? this.cancelNote() : null)"/>
                            <div class="mz-mgate__actions">
                                <button type="button" class="mz-btn mz-btn--secondary"
                                        t-on-click="() => this.cancelNote()" t-esc="cancelLabel"/>
                                <button type="button" class="mz-btn mz-btn--primary"
                                        data-testid="mz-note-save"
                                        t-on-click="() => this.saveNote()" t-esc="saveNoteLabel"/>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Manager approval for a comp or a void. Modal, because approving is a
                     deliberate act by a DIFFERENT person than the cashier. -->
                <div t-if="state.managerGate" class="mz-modal" role="dialog" aria-modal="true"
                     t-att-aria-label="state.managerGate.title">
                    <div class="mz-modal__panel mz-modal__panel--sm">
                        <ManagerGate action="state.managerGate.action"
                                     heading="state.managerGate.title"
                                     detail="state.managerGate.detail"
                                     reasonRequired="state.managerGate.reasonRequired"
                                     api="api" configId="boot.config_id"
                                     onApproved.bind="runManagerGate"
                                     onCancel.bind="cancelManagerGate"/>
                    </div>
                </div>
                <div t-if="state.actionError" class="mz-alert mz-alert--danger mz-action-err"
                     role="alert" data-testid="mz-action-error">
                    <span class="mz-alert__content" t-esc="state.actionError"/>
                    <button type="button" class="mz-btn mz-btn--sm mz-btn--secondary"
                            t-on-click="() => this.state.actionError = ''">✕</button>
                </div>
                <!-- R1B Undo toast: a removed cart line is briefly restorable. Non-blocking,
                     keyboard-accessible, screen-reader announced. Cart edits only. -->
                <div t-if="cart.undo" class="mz-undo-toast" role="status" aria-live="polite">
                    <span class="mz-undo-msg" t-esc="undoMsg"/>
                    <button class="mz-btn mz-btn--sm mz-undo-btn" t-on-click="() => this.onUndo()" t-esc="undoLabel"/>
                    <button class="mz-undo-x" t-att-aria-label="'Dismiss'" t-on-click="() => this.dismissUndo()">✕</button>
                </div>

                <!-- R2A CP6/7: table picker on REAL /floors data. 'assign' mode (CP6):
                     only free tables selectable. 'move' mode (CP7): free → transfer,
                     occupied → merge; reserved + the source table are not selectable. -->
                <!-- CP10: the table picker modal moved to app-level (below) so it also
                     serves reservation/waitlist SEATING, not only the menu phase. -->

                <!-- R2A CP7: Transfer / Merge confirmation — the consequence is explicit
                     BEFORE it runs; merge shows source·destination·combined guests+totals;
                     a payment-blocked merge is explained (no casual override). -->
                <div t-if="state.moveConfirm" class="mz-modal-scrim" t-on-click="() => this.cancelMove()">
                    <div class="mz-modal mz-move" role="dialog" aria-modal="true" aria-label="Confirm move"
                         t-on-click.stop="() => {}">
                        <div class="mz-modal__head">
                            <span class="mz-modal__title">
                                <t t-if="state.moveConfirm.kind === 'transfer'">Transfer order</t>
                                <t t-else="">Merge tables</t>
                            </span>
                            <button class="mz-modal__x" aria-label="Close" t-on-click="() => this.cancelMove()">✕</button>
                        </div>

                        <!-- source → destination -->
                        <div class="mz-move__route">
                            <span class="mz-move__t">T<t t-esc="state.moveConfirm.srcName"/></span>
                            <span class="mz-move__arrow mz-dirglyph" aria-hidden="true">→</span>
                            <span class="mz-move__t">T<t t-esc="state.moveConfirm.dest.name"/></span>
                        </div>

                        <t t-if="state.moveConfirm.kind === 'transfer'">
                            <p class="mz-move__say" t-esc="moveConfirmSay"/>
                        </t>
                        <t t-else="">
                            <!-- merge: show both sides + the combined result -->
                            <table class="mz-move__grid">
                                <tr><th></th><th>Guests</th><th>Total</th></tr>
                                <tr><td>Source T<t t-esc="state.moveConfirm.srcName"/></td>
                                    <td class="mz-num" t-esc="state.moveConfirm.srcGuests"/>
                                    <td class="mz-num" t-esc="fmt(state.moveConfirm.srcTotal)"/></tr>
                                <tr><td>Dest T<t t-esc="state.moveConfirm.dest.name"/></td>
                                    <td class="mz-num" t-esc="state.moveConfirm.dstGuests"/>
                                    <td class="mz-num" t-esc="fmt(state.moveConfirm.dstTotal)"/></tr>
                                <tr class="mz-move__sum"><td>Combined</td>
                                    <td class="mz-num" t-esc="combinedGuests"/>
                                    <td class="mz-num" t-esc="fmt(combinedTotal)"/></tr>
                            </table>
                        </t>

                        <div t-if="state.moveConfirm.error"
                             t-att-class="'mz-alert ' + (state.moveConfirm.blocked ? 'mz-alert--danger' : 'mz-alert--warning')"
                             role="alert"><span class="mz-alert__content" t-esc="state.moveConfirm.error"/></div>

                        <div class="mz-move__actions">
                            <button class="mz-btn mz-btn--secondary" t-on-click="() => this.cancelMove()">Cancel</button>
                            <button t-if="!state.moveConfirm.blocked" class="mz-btn mz-btn--primary"
                                    t-att-disabled="state.inFlight ? 'disabled' : false"
                                    t-on-click="() => this.confirmMove()">
                                <t t-if="state.moveConfirm.kind === 'transfer'">Move to T<t t-esc="state.moveConfirm.dest.name"/></t>
                                <t t-else="">Merge into T<t t-esc="state.moveConfirm.dest.name"/></t>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- R2A CP9: recalling another order while the current one has real
                     content — the current order is PARKED (never silently lost), then
                     the chosen order opens. Explicit verbs, not OK/Confirm. -->
                <div t-if="state.recallConfirm" class="mz-modal-scrim" t-on-click="() => this.cancelRecall()">
                    <div class="mz-modal mz-recall" role="dialog" aria-modal="true" aria-label="Open another order"
                         t-on-click.stop="() => {}">
                        <div class="mz-modal__head">
                            <span class="mz-modal__title">Park current order?</span>
                            <button class="mz-modal__x" aria-label="Close" t-on-click="() => this.cancelRecall()">✕</button>
                        </div>
                        <p class="mz-move__say" t-esc="recallConfirmSay"/>
                        <div class="mz-move__actions">
                            <button class="mz-btn mz-btn--secondary" t-on-click="() => this.cancelRecall()">Cancel</button>
                            <button class="mz-btn mz-btn--primary"
                                    t-att-disabled="state.inFlight ? 'disabled' : false"
                                    t-on-click="() => this.confirmRecall()">Park &amp; open</button>
                        </div>
                    </div>
                </div>
            </t>

            <!-- PAYMENT -->
            <t t-elif="state.phase === 'payment'">
                <PaymentScreen payment="state.payment" currency="currency" methods="state.methods"
                               inFlight="state.inFlight" tenderError="state.tenderError"
                               warn="state.warn" managerReq="state.managerReq" terminal="state.terminal" qr="state.qr"
                               customer="state.customer" creditWarn="state.creditWarn"
                               creditManager="state.creditManager" customerPicker="state.customerPicker"
                               cashmachine="state.cashmachine"
                               onTender="(p) => this.submitTender(p)"
                               onWarnContinue="() => this.warnContinue()"
                               onWarnCancel="() => this.warnCancel()"
                               onManagerApprove="(a) => this.managerApprove(a)"
                               onManagerCancel="() => this.managerCancel()"
                               onCustomerOpen="() => this.openCustomerPicker()"
                               onCustomerClose="() => this.closeCustomerPicker()"
                               onCustomerSearch="(q) => this.searchCustomers(q)"
                               onCustomerChoose="(c) => this.chooseCustomer(c)"
                               onCustomerClear="() => this.clearCustomer()"
                               onAccountService="(k) => this.accountService(k)"
                               onCreditWarnContinue="() => this.creditWarnContinue()"
                               onCreditWarnCancel="() => this.creditWarnCancel()"
                               onCreditManagerApprove="(a) => this.creditManagerApprove(a)"
                               onCreditManagerCancel="() => this.creditManagerCancel()"
                               onTerminalSelect="(p) => this.onTerminalSelect(p)"
                               onTerminalSend="(p) => this.startTerminal(p)"
                               onTerminalCancel="(o) => this.terminalCancel(o)"
                               onTerminalRetry="() => this.terminalRetry()"
                               onTerminalForceDone="(a) => this.terminalForceDone(a)"
                               onCashMachineSelect="(p) => this.onCashMachineSelect(p)"
                               onCashMachineSend="() => this.startCashMachine()"
                               onCashMachineCancel="(o) => this.cashMachineCancel(o)"
                               onCashMachineRetry="() => this.cashMachineRetry()"
                               onCashMachineForceDone="(a) => this.cashMachineForceDone(a)"
                               onQrSelect="(p) => this.onQrSelect(p)"
                               onQrConfirm="() => this.qrConfirm()"
                               onQrCancel="(o) => this.qrCancel(o)"
                               onQrRetry="() => this.qrRetry()"
                               onBack="() => this.backToMenu()"/>
            </t>

            <!-- PROCESSING -->
            <t t-elif="state.phase === 'processing'">
                <div class="mz-state mz-state--info">
                    <div class="mz-spinner"/>
                    <p>Processing payment…</p>
                </div>
            </t>

            <!-- RECEIPT -->
            <t t-elif="state.phase === 'receipt'">
                <Receipt receipt="state.receipt" currency="currency"
                         onNewOrder="() => this.newOrder()"/>
            </t>

            <!-- R2A CP9: ORDERS WORKSPACE — find/recall real orders (Open|Parked|
                 Completed) + search. Reuses pos.order as truth; never a second engine. -->
            <t t-elif="state.phase === 'orders'">
                <section class="mz-orders">
                    <div class="mz-orders__bar">
                        <input type="text" class="mz-search mz-orders__search"
                               placeholder="Search orders — reference, table, or customer"
                               aria-label="Search orders"
                               t-on-input="(ev) => this.onOrdersSearch(ev)"/>
                        <nav class="mz-orders__tabs" role="group" aria-label="Order filter">
                            <button class="mz-tab" t-att-class="{'mz-tab--active': ordersState.filter === 'open'}"
                                    t-att-aria-pressed="(ordersState.filter === 'open') ? 'true' : 'false'"
                                    t-on-click="() => this.setOrdersFilter('open')">Open</button>
                            <button class="mz-tab" t-att-class="{'mz-tab--active': ordersState.filter === 'parked'}"
                                    t-att-aria-pressed="(ordersState.filter === 'parked') ? 'true' : 'false'"
                                    t-on-click="() => this.setOrdersFilter('parked')">Parked</button>
                            <button class="mz-tab" t-att-class="{'mz-tab--active': ordersState.filter === 'completed'}"
                                    t-att-aria-pressed="(ordersState.filter === 'completed') ? 'true' : 'false'"
                                    t-on-click="() => this.setOrdersFilter('completed')">Completed</button>
                        </nav>
                    </div>

                    <div t-if="ordersState.error" class="mz-alert mz-alert--warning" role="status">
                        <span class="mz-alert__content" t-esc="ordersState.error"/>
                        <span class="mz-alert__actions"><button class="mz-btn mz-btn--sm mz-btn--secondary" t-on-click="() => this.loadOrders(true)">Retry</button></span>
                    </div>

                    <t t-if="ordersState.loading and !ordersState.rows.length">
                        <div class="mz-state mz-state--info"><div class="mz-spinner"/><p>Loading orders…</p></div>
                    </t>
                    <t t-elif="!ordersState.rows.length and !ordersState.error">
                        <div class="mz-state mz-state--empty" t-esc="ordersEmptyLabel"/>
                    </t>
                    <t t-else="">
                        <div class="mz-orders__list">
                            <button t-foreach="ordersState.rows" t-as="o" t-key="o.uuid"
                                    class="mz-ordcard" t-att-class="{'mz-ordcard--done': o.completed}"
                                    t-on-click="() => this.onRecall(o)">
                                <div class="mz-ordcard__top">
                                    <span class="mz-ordcard__ref">
                                        <t t-if="o.table">T<t t-esc="o.table"/></t>
                                        <t t-else="" t-esc="orderTypeLabel(o.order_type)"/>
                                    </span>
                                    <span t-attf-class="mz-status mz-status--sm mz-status--{{o.completed ? 'ok' : (o.parked ? 'warn' : 'info')}}">
                                        <span class="mz-status__dot"/>
                                        <t t-if="o.completed">Completed</t>
                                        <t t-elif="o.parked">Parked</t>
                                        <t t-else="">Open</t>
                                    </span>
                                </div>
                                <div class="mz-ordcard__mid">
                                    <span class="mz-ordcard__meta" t-esc="o.pos_reference"/>
                                    <span t-if="o.partner" class="mz-ordcard__meta" t-esc="o.partner"/>
                                    <span t-if="o.guests" class="mz-ordcard__meta"><t t-esc="o.guests"/> guests</span>
                                </div>
                                <div class="mz-ordcard__foot">
                                    <span class="mz-ordcard__total mz-num" t-esc="fmt(o.amount_total)"/>
                                    <span t-if="o.remaining > 0 and !o.completed" class="mz-ordcard__due mz-num">
                                        <t t-esc="fmt(o.amount_paid)"/> paid · <t t-esc="fmt(o.remaining)"/> due
                                    </span>
                                </div>
                            </button>
                        </div>
                        <button t-if="ordersState.hasMore" class="mz-btn mz-btn--secondary mz-orders__more"
                                t-att-disabled="ordersState.loading ? 'disabled' : false"
                                t-on-click="() => this.loadMoreOrders()">Load more</button>
                    </t>
                </section>
            </t>

            <!-- R2A CP9: COMPLETED ORDER — READ-ONLY. No add/edit/move/send/charge/park;
                 it can never be resurrected into an editable draft. -->
            <t t-elif="state.phase === 'completed'">
                <section class="mz-completed" t-if="state.completedView">
                    <div class="mz-completed__head">
                        <button class="mz-btn mz-btn--sm mz-btn--secondary" t-on-click="() => this.closeCompleted()"><span class="mz-dirglyph" aria-hidden="true">←</span> Orders</button>
                        <span class="mz-completed__ref" t-esc="state.completedView.pos_reference"/>
                        <span class="mz-status mz-status--sm mz-status--ok"><span class="mz-status__dot"/>Completed</span>
                    </div>
                    <div class="mz-completed__summary">
                        <span t-if="state.completedView.table">Table T<t t-esc="state.completedView.table"/></span>
                        <span t-else="" t-esc="orderTypeLabel(state.completedView.order_type)"/>
                        <span t-if="state.completedView.partner" t-esc="state.completedView.partner.name"/>
                        <span t-if="state.completedView.date_order" t-esc="state.completedView.date_order"/>
                    </div>
                    <div class="mz-completed__lines">
                        <div t-foreach="state.completedView.lines" t-as="l" t-key="l_index" class="mz-completed__line">
                            <span class="mz-completed__q mz-num" t-esc="l.qty"/>
                            <span class="mz-completed__n" t-esc="l.name"/>
                            <span class="mz-completed__p mz-num" t-esc="fmt(l.price_unit * l.qty)"/>
                        </div>
                    </div>
                    <div class="mz-completed__total">
                        <span>Total</span>
                        <span class="mz-num" t-esc="fmt(state.completedView.amount_total)"/>
                    </div>
                    <p class="mz-completed__note">This order is completed and read-only.</p>
                </section>
            </t>

            <!-- R2A CP10: RESERVATIONS + WAITLIST (host) workspace — arrival & seating -->
            <t t-elif="state.phase === 'reservations'">
                <section class="mz-host">
                    <div class="mz-host__bar">
                        <nav class="mz-host__date" role="group" aria-label="Day">
                            <button class="mz-tab" t-att-class="{'mz-tab--active': hostState.dateOffset === -1}"
                                    t-att-aria-pressed="(hostState.dateOffset === -1) ? 'true' : 'false'"
                                    t-on-click="() => this.setHostDate(-1)">Yesterday</button>
                            <button class="mz-tab" t-att-class="{'mz-tab--active': hostState.dateOffset === 0}"
                                    t-att-aria-pressed="(hostState.dateOffset === 0) ? 'true' : 'false'"
                                    t-on-click="() => this.setHostDate(0)">Today</button>
                            <button class="mz-tab" t-att-class="{'mz-tab--active': hostState.dateOffset === 1}"
                                    t-att-aria-pressed="(hostState.dateOffset === 1) ? 'true' : 'false'"
                                    t-on-click="() => this.setHostDate(1)">Tomorrow</button>
                        </nav>
                        <button t-if="hostState.tab === 'reservations'" class="mz-btn mz-btn--secondary"
                                t-on-click="() => this.openResForm()">+ New reservation</button>
                        <button t-else="" class="mz-btn mz-btn--secondary"
                                t-on-click="() => this.openWlForm()">+ Add walk-in</button>
                    </div>
                    <div class="mz-host__bar">
                        <input type="text" class="mz-search mz-host__search"
                               placeholder="Search name, phone or reference"
                               aria-label="Search reservations"
                               t-on-input="(ev) => this.onHostSearch(ev)"/>
                        <nav class="mz-host__tabs" role="group" aria-label="View">
                            <button class="mz-tab" t-att-class="{'mz-tab--active': hostState.tab === 'reservations'}"
                                    t-att-aria-pressed="(hostState.tab === 'reservations') ? 'true' : 'false'"
                                    t-on-click="() => this.setHostTab('reservations')">Reservations</button>
                            <button class="mz-tab" t-att-class="{'mz-tab--active': hostState.tab === 'waitlist'}"
                                    t-att-aria-pressed="(hostState.tab === 'waitlist') ? 'true' : 'false'"
                                    t-on-click="() => this.setHostTab('waitlist')">Waitlist</button>
                        </nav>
                    </div>

                    <div t-if="hostState.error" class="mz-alert mz-alert--warning" role="status">
                        <span class="mz-alert__content" t-esc="hostState.error"/>
                        <span class="mz-alert__actions"><button class="mz-btn mz-btn--sm mz-btn--secondary" t-on-click="() => this.loadHost()">Retry</button></span>
                    </div>

                    <!-- RESERVATIONS tab -->
                    <t t-if="hostState.tab === 'reservations'">
                        <t t-if="hostState.loading and !hostState.resRows.length">
                            <div class="mz-state mz-state--info"><div class="mz-spinner"/><p>Loading reservations…</p></div>
                        </t>
                        <t t-elif="!hostState.resRows.length">
                            <div class="mz-state mz-state--empty" t-esc="hostEmptyLabel"/>
                        </t>
                        <t t-else="">
                            <div class="mz-host__list">
                                <div t-foreach="hostState.resRows" t-as="r" t-key="r.id" class="mz-rescard">
                                    <div class="mz-rescard__top">
                                        <span class="mz-rescard__time mz-num" t-esc="r.time"/>
                                        <span t-attf-class="mz-status mz-status--sm mz-status--{{statusVariant(r.state)}}">
                                            <span class="mz-status__dot"/><t t-esc="resStatusLabel(r.state)"/>
                                        </span>
                                    </div>
                                    <div class="mz-rescard__who">
                                        <span class="mz-rescard__name" t-esc="r.who"/>
                                        <span class="mz-rescard__guests"><t t-esc="r.guests"/> guests</span>
                                    </div>
                                    <div class="mz-rescard__meta">
                                        <span t-if="r.table_id" class="mz-badge" t-esc="r.table"/>
                                        <span t-else="" class="mz-badge mz-badge--muted">No table</span>
                                        <span t-if="r.is_vip" class="mz-badge mz-badge--vip">VIP</span>
                                        <span t-if="r.occasion" class="mz-badge" t-esc="r.occasion"/>
                                        <span t-if="r.late" class="mz-status mz-status--warning mz-status--sm">
                                            <span class="mz-status__dot"/>Late</span>
                                        <span t-if="r.state === 'seated' and r.table" class="mz-badge">Seated · <t t-esc="r.table"/></span>
                                    </div>
                                    <div t-if="r.phone" class="mz-rescard__phone mz-ltr" t-esc="r.phone"/>
                                    <div class="mz-rescard__actions">
                                        <button t-foreach="resActions(r)" t-as="a" t-key="a.action"
                                                t-attf-class="mz-btn mz-btn--sm mz-btn--{{a.kind}}"
                                                t-att-disabled="state.inFlight ? 'disabled' : false"
                                                t-on-click="() => this.onResAction(r, a.action)" t-esc="a.label"/>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </t>

                    <!-- WAITLIST tab -->
                    <t t-else="">
                        <div t-if="hostState.wlStats" class="mz-host__stats">
                            <span><t t-esc="hostState.wlStats.waiting"/> waiting</span>
                            <span><t t-esc="hostState.wlStats.covers"/> covers</span>
                            <span t-if="hostState.wlStats.quote">~<t t-esc="hostState.wlStats.quote"/> min quote</span>
                        </div>
                        <t t-if="hostState.loading and !hostState.wlRows.length">
                            <div class="mz-state mz-state--info"><div class="mz-spinner"/><p>Loading waitlist…</p></div>
                        </t>
                        <t t-elif="!hostState.wlRows.length">
                            <div class="mz-state mz-state--empty" t-esc="hostEmptyLabel"/>
                        </t>
                        <t t-else="">
                            <div class="mz-host__list">
                                <div t-foreach="hostState.wlRows" t-as="w" t-key="w.id" class="mz-rescard">
                                    <div class="mz-rescard__top">
                                        <span class="mz-rescard__name" t-esc="w.who"/>
                                        <span t-attf-class="mz-status mz-status--sm mz-status--{{statusVariant(w.state)}}">
                                            <span class="mz-status__dot"/><t t-esc="wlStatusLabel(w.state)"/>
                                        </span>
                                    </div>
                                    <div class="mz-rescard__meta">
                                        <span class="mz-rescard__guests"><t t-esc="w.party_size"/> guests</span>
                                        <span t-if="w.quoted_wait" class="mz-badge">Quoted <t t-esc="w.quoted_wait"/> min</span>
                                        <span t-if="w.over" class="mz-status mz-status--warning mz-status--sm">
                                            <span class="mz-status__dot"/>Waiting <t t-esc="w.waited"/> min</span>
                                        <span t-else="" class="mz-badge">Waiting <t t-esc="w.waited"/> min</span>
                                        <span t-if="w.table" class="mz-badge">Seated · T<t t-esc="w.table"/></span>
                                    </div>
                                    <div t-if="w.phone" class="mz-rescard__phone mz-ltr" t-esc="w.phone"/>
                                    <div class="mz-rescard__actions">
                                        <button t-foreach="wlActions(w)" t-as="a" t-key="a.action"
                                                t-attf-class="mz-btn mz-btn--sm mz-btn--{{a.kind}}"
                                                t-att-disabled="state.inFlight ? 'disabled' : false"
                                                t-on-click="() => this.onWlAction(w, a.action)" t-esc="a.label"/>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </t>
                </section>
            </t>

            <!-- CP10: table picker (app-level) — assign (CP6) / move (CP7) / seat (CP10) -->
            <div t-if="state.assignPicker" class="mz-modal-scrim" t-on-click="() => this.closeAssignPicker()">
                <div class="mz-modal mz-assign" role="dialog" aria-modal="true"
                     t-att-aria-label="state.assignPicker.mode === 'move' ? 'Move table' : (state.assignPicker.mode === 'seat' ? 'Seat guest' : 'Assign table')"
                     t-on-click.stop="() => {}">
                    <div class="mz-modal__head">
                        <span class="mz-modal__title" t-esc="assignPickerTitle"/>
                        <button class="mz-modal__x" aria-label="Close" t-on-click="() => this.closeAssignPicker()">✕</button>
                    </div>
                    <t t-if="state.assignPicker.busy">
                        <div class="mz-state mz-state--info"><div class="mz-spinner"/></div>
                    </t>
                    <t t-else="">
                        <nav class="mz-floortabs" aria-label="Floors">
                            <button t-foreach="state.assignPicker.floors" t-as="fl" t-key="fl.id"
                                    class="mz-floortab mz-filter-chip"
                                    t-att-class="{'mz-floortab--active': fl.id === (assignFloor and assignFloor.id)}"
                                    t-att-aria-pressed="(fl.id === (assignFloor and assignFloor.id)) ? 'true' : 'false'"
                                    t-on-click="() => this.selectAssignFloor(fl.id)" t-esc="fl.name"/>
                        </nav>
                        <div t-if="state.assignPicker.error" class="mz-alert mz-alert--danger" role="alert">
                             <span class="mz-alert__content" t-esc="state.assignPicker.error"/></div>
                        <div class="mz-assign__grid">
                            <button t-foreach="assignTables" t-as="t" t-key="t.id"
                                    class="mz-assign__t"
                                    t-att-class="'mz-assign__t--' + t.status"
                                    t-att-disabled="pickerTableDisabled(t) ? 'disabled' : false"
                                    t-att-aria-label="'Table ' + t.name + ', ' + t.status + ', ' + t.seats + ' seats'"
                                    t-on-click="() => this.onPickTable(t)">
                                <span class="mz-assign__num" t-esc="t.name"/>
                                <span class="mz-assign__meta">
                                    <t t-if="isSourceTable(t)">Current table</t>
                                    <t t-elif="t.status === 'available'">Open · <t t-esc="t.seats"/>p</t>
                                    <t t-elif="t.status === 'reserved'">Reserved</t>
                                    <t t-elif="state.assignPicker.mode === 'move'">Occupied · merge</t>
                                    <t t-else="">Occupied</t>
                                </span>
                            </button>
                        </div>
                    </t>
                </div>
            </div>

            <!-- CP10: New reservation form -->
            <div t-if="state.resForm" class="mz-modal-scrim" t-on-click="() => this.closeResForm()">
                <div class="mz-modal mz-resform" role="dialog" aria-modal="true" aria-label="New reservation" t-on-click.stop="() => {}">
                    <div class="mz-modal__head">
                        <span class="mz-modal__title">New reservation</span>
                        <button class="mz-modal__x" aria-label="Close" t-on-click="() => this.closeResForm()">✕</button>
                    </div>
                    <label class="mz-field"><span>Guest name *</span>
                        <input type="text" class="mz-input" t-model="state.resForm.name"/></label>
                    <label class="mz-field"><span>Phone</span>
                        <input type="tel" class="mz-input mz-ltr" t-model="state.resForm.phone"/></label>
                    <div class="mz-field"><span>Party size</span>
                        <span class="mz-stepper mz-guest" role="group" aria-label="Party size">
                            <button type="button" class="mz-stepper__btn" aria-label="Fewer guests"
                                    t-on-click="() => this.resFormGuests(-1)">−</button>
                            <span class="mz-stepper__value" t-esc="state.resForm.guests"/>
                            <button type="button" class="mz-stepper__btn" aria-label="More guests"
                                    t-on-click="() => this.resFormGuests(1)">+</button>
                        </span></div>
                    <div class="mz-field mz-field--row">
                        <label><span>Date</span><input type="date" class="mz-input" t-model="state.resForm.date"
                                t-on-change="() => this._loadResFormTables()"/></label>
                        <label><span>Time</span><input type="time" class="mz-input" t-model="state.resForm.time"
                                t-on-change="() => this._loadResFormTables()"/></label>
                    </div>
                    <label class="mz-field"><span>Table *</span>
                        <select class="mz-select" t-model.number="state.resForm.table_id">
                            <option value="">Choose an available table…</option>
                            <option t-foreach="state.resForm.tables" t-as="t" t-key="t.id" t-att-value="t.id">
                                T<t t-esc="t.table"/> · <t t-esc="t.seats"/>p · <t t-esc="t.floor"/></option>
                        </select></label>
                    <label class="mz-field"><span>Note</span>
                        <input type="text" class="mz-input" t-model="state.resForm.note"/></label>
                    <div t-if="state.resForm.error" class="mz-alert mz-alert--danger mz-alert--inline" role="alert">
                        <span class="mz-alert__content" t-esc="state.resForm.error"/></div>
                    <div class="mz-move__actions">
                        <button class="mz-btn mz-btn--secondary" t-on-click="() => this.closeResForm()">Cancel</button>
                        <button class="mz-btn mz-btn--primary" t-att-disabled="state.resForm.busy ? 'disabled' : false"
                                t-on-click="() => this.saveReservation()">Save reservation</button>
                    </div>
                </div>
            </div>

            <!-- CP10: Add walk-in form -->
            <div t-if="state.wlForm" class="mz-modal-scrim" t-on-click="() => this.closeWlForm()">
                <div class="mz-modal mz-resform" role="dialog" aria-modal="true" aria-label="Add walk-in" t-on-click.stop="() => {}">
                    <div class="mz-modal__head">
                        <span class="mz-modal__title">Add walk-in</span>
                        <button class="mz-modal__x" aria-label="Close" t-on-click="() => this.closeWlForm()">✕</button>
                    </div>
                    <label class="mz-field"><span>Guest name *</span>
                        <input type="text" class="mz-input" t-model="state.wlForm.name"/></label>
                    <div class="mz-field"><span>Party size</span>
                        <span class="mz-stepper mz-guest" role="group" aria-label="Party size">
                            <button type="button" class="mz-stepper__btn" aria-label="Fewer guests"
                                    t-on-click="() => this.wlFormSize(-1)">−</button>
                            <span class="mz-stepper__value" t-esc="state.wlForm.party_size"/>
                            <button type="button" class="mz-stepper__btn" aria-label="More guests"
                                    t-on-click="() => this.wlFormSize(1)">+</button>
                        </span></div>
                    <label class="mz-field"><span>Phone</span>
                        <input type="tel" class="mz-input mz-ltr" t-model="state.wlForm.phone"/></label>
                    <label class="mz-field"><span>Quoted wait (min)</span>
                        <input type="number" class="mz-input mz-num" t-model="state.wlForm.quoted_wait"
                               placeholder="auto"/></label>
                    <label class="mz-field"><span>Note</span>
                        <input type="text" class="mz-input" t-model="state.wlForm.note"/></label>
                    <div t-if="state.wlForm.error" class="mz-alert mz-alert--danger mz-alert--inline" role="alert">
                        <span class="mz-alert__content" t-esc="state.wlForm.error"/></div>
                    <div class="mz-move__actions">
                        <button class="mz-btn mz-btn--secondary" t-on-click="() => this.closeWlForm()">Cancel</button>
                        <button class="mz-btn mz-btn--primary" t-att-disabled="state.wlForm.busy ? 'disabled' : false"
                                t-on-click="() => this.saveWalkIn()">Add to waitlist</button>
                    </div>
                </div>
            </div>

            <!-- CP10: no-show / cancel confirmation -->
            <div t-if="state.hostConfirm" class="mz-modal-scrim" t-on-click="() => this.cancelHostConfirm()">
                <div class="mz-modal mz-recall" role="dialog" aria-modal="true" aria-label="Confirm" t-on-click.stop="() => {}">
                    <div class="mz-modal__head">
                        <span class="mz-modal__title" t-esc="hostConfirmLabel"/>
                        <button class="mz-modal__x" aria-label="Close" t-on-click="() => this.cancelHostConfirm()">✕</button>
                    </div>
                    <div class="mz-move__actions">
                        <button class="mz-btn mz-btn--secondary" t-on-click="() => this.cancelHostConfirm()">Cancel</button>
                        <button class="mz-btn mz-btn--danger"
                                t-att-disabled="state.inFlight ? 'disabled' : false"
                                t-on-click="() => this.confirmHostConfirm()">
                            <t t-if="state.hostConfirm.kind === 'no_show'">Mark no-show</t>
                            <t t-else="">Confirm cancel</t>
                        </button>
                    </div>
                </div>
            </div>

            </div><!-- /.mz-shell -->
        </div>
    </t>

</templates>
