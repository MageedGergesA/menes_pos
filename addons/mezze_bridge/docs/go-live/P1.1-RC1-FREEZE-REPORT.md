# P1.1 — Release Candidate Freeze & On-Site Pilot Gate — FINAL REPORT

Release: **Mezze POS — Controlled Pilot RC1** · tag `mezze-pilot-rc1` → identity commit `634d17e`
(release-content commit `277338b`) · branch `main` · module mezze_bridge 19.0.1.8.0.

## 1. Working-tree audit
`git status --short`, `git diff --stat`, `git diff --check`, `git ls-files --others` were run in
`/home/mageed/odoo_work_19/mezze`. Finding: the entire mezze_bridge implementation was **untracked** —
the prior HEAD (`ce8dc74`) held only a skeleton, so RC1 is the module's first full commit (216 files).
`git diff --check`: clean. Classification of every path: production code (models/controllers/domain/
security/manifest/requirements), migrations, tests, documentation, generated evidence (go-live +
acceptance JPGs), development artifacts (root `CLAUDE.md`, `tests/concurrency/` drivers), no secrets,
no temp/db/log files staged.

## 2. Files included
216 files: docs 100 · tests 37 · static 28 (incl. 59 JPG evidence total across docs) · models 20 ·
domain 14 · controllers 7 · migrations 5 · data 2 · security 1 · requirements.txt · __manifest__.py.
The complete automated suite (`tests/test_*.py`, incl. `test_runtime_outbox_concurrency.py`) is included.

## 3. Files excluded
- `tests/concurrency/` — standalone reproducibility drivers embedding session-local `/tmp/claude…` +
  `/home/mageed` absolute paths; **not** imported by the 218-test suite.
- root `CLAUDE.md` — Claude Code dev tooling.
- raw `release-test.log` — embeds local absolute paths + per-line trailing whitespace; replaced by a
  clean distilled `release-test-result.txt`.
- `__pycache__`/`.pyc` — git-ignored, none staged. Database backups — kept in scratchpad only.

## 4. Secret scan
Scanned all shippable code for secret/key/token/PEM/AWS patterns. **No real secrets.** Every match is
either install-time random-token generation (`secrets.token_urlsafe(32)` in the post-init hook) or a
**fake test fixture** (`'sekret'`, `'agg-secret-xyz'`, `'live-agg-secret-1'`). `MEZZE_MASTER_KEY` is read
from the environment only; aggregator/webhook secrets are AES-GCM enveloped at rest. No `.env`, dump,
key, or credential file present or staged.

## 5. Release scope
Exactly the completed pilot scope: counter POS, dine-in, reservations, walk-ins/waitlist, table transfer,
financially-safe merge, courses/KDS, partial/mixed payment, refunds, QR ordering, pickup, pay-on-delivery,
prepaid aggregator ingestion, customer order status, design system, User Settings, Admin Console,
operational validation, runbooks + go-live evidence.

## 6. Disabled capabilities (verified)
- **Online card payment** — genuinely **absent** (no PSP/redirect route exists; grep-confirmed). Unreachable.
- **Seat-level identity / split-by-seat** — genuinely **absent** (not modelled). Unreachable.
- **Drive-thru** — present in code; all three `/drivethru/*` handlers are `self._authorize()`-gated
  (authenticated terminal only, not anonymous public) and not surfaced in pilot navigation. **Honest
  classification: operationally disabled (hidden), NOT hard-flag-blocked** (limitation D-1). Mitigation:
  do not provision a drive-thru client for the pilot branch. A hard server gate is a candidate RC2 patch.
- **Advanced driver dispatch** — partial; driver assignment is a documented manual procedure.
- Unverified providers/aggregators kept inactive (no secret → validator flags).

## 7. Pre-commit test result
`odoo-bin -d mezze_test --test-enable --test-tags mezze_invariants,mezze_runtime --stop-after-init
--no-http --log-level=test` → **exit code 0**, **`0 failed, 0 error(s) of 218 tests`**, 17.87s, 20,726
queries. Distilled to `release-test-result.txt`. Expected negative-path DB warnings (duplicate-nonce /
duplicate-idempotency-key rejection, best-effort audit-append failing soft on read-only routes) are
documented as NOT failures.

## 8. Production-validator result
`mezze.golive.validator` under the **production profile**: **0 FAIL**, 1 WARNING (`company_timezone`
unset — an on-site environment prerequisite). All hardening checks PASS: shared-admin disabled, master
key present, `api_security=enforce`, HTTPS base_url, aggregator secrets present, status-token TTL, no
dead-letters. Evidence: `configuration/rc1-production-validation.txt`.

## 9. Financial reconciliation
Genuine payment-engine orders: **294** (payments 297). **overpaid 0 · orphan 0 · unexplained
difference 0.** 89 fixture-only orders (amount_paid set, zero payment rows) explicitly excluded and
identified — not mixed with genuine records. Evidence: `financial-reconciliation/rc1-reconciliation.txt`.

## 10. Backup and restore
`pg_dump -Fc` → `pg_restore --no-owner` into a clean DB. **0 row loss across 17 critical tables**
(res_users, product_product, pos_order 400, pos_order_line 645, pos_payment 301, reversal, reservation,
waitlist, restaurant_table, kds_ticket, outbox_event, config_value, terminal, config_template,
config_assignment, audit_log 8205, aggregator). Backup 1.06s · restore 13.35s · 7.6M. `pg_restore` rc=1
is expected `--no-owner` ACL warning noise; the exact count match is authoritative. Backup file kept out
of git. Evidence: `backup-restore/rc1-backup-restore.txt`.

## 11. Release commit
`277338b` — "release: Mezze POS controlled pilot RC1". Staged via scoped `git add addons/mezze_bridge`
then `git reset` on `tests/concurrency`; `git diff --cached --check` clean; reviewed by category before commit.

## 12. Release tag
Annotated `mezze-pilot-rc1` → identity commit `634d17e` (records the manifest; code/tests/assets identical
to `277338b`). `git show --stat` confirms; working tree clean apart from the two by-design exclusions.
**Not pushed** (branch/tag push remains the repository owner's action).

## 13. Tagged-worktree installation
`git worktree add /tmp/mezze-pilot-rc1 mezze-pilot-rc1` → HEAD `634d17e`. Fresh install
(`-i mezze_bridge` into an empty DB) from the tag, addons_path pointing at the worktree.
**Result: clean install SUCCEEDED** — modules loaded, registry loaded, 0 fatal import/parse errors; the
excluded `tests/concurrency/` and `CLAUDE.md` are absent yet nothing is missing. Environment note: an
initial attempt failed on an unrelated half-deleted `enterprise_19e/web_enterprise` file in the workspace
addons path (not RC code); mezze_bridge depends only on community modules, so that path was excluded and
the install re-run cleanly. Evidence: `configuration/rc1-tagged-worktree-verification.txt`.

## 14. Tagged-worktree test result
- **TAG code vs the canonical provisioned DB: exit 0, `0 failed, 0 error(s) of 218 tests`** — proves the
  frozen tag reproduces the verified result with **no dependency on uncommitted files** (evidence:
  `configuration/rc1-tagged-worktree-canonical.txt`).
- **Bare clean install: NOT green** — 108 errors (no provisioning) → 55 errors (after a minimal config),
  all POS session/config-isolation + menu/tax provisioning artifacts (`UserError: assign a PoS`,
  `ValidationError: another session already open`), **not code defects**. Root cause: the runtime test
  suite is **NOT hermetic** — it assumes a pre-provisioned POS environment that install/demo do not create.
- **Honest §12 status:** the tag installs cleanly and reproduces 218-green against a provisioned DB, but
  "full test success from a **bare** clean install" is **NOT achieved**. Classified a **MAJOR
  test-infrastructure limitation** (self-provisioning test base is the fix — test-only, RC2 candidate), not
  a Critical/financial pilot blocker.

## 15. Release manifest
`docs/go-live/release-manifest.md` records: release name, RC1, timestamp, branch, exact commit + tag,
Odoo/Python/PostgreSQL/OS versions, module + migration version, enabled vs disabled scope, test count,
reconciliation/validator/backup results, status-token policy, asset + design-token checksums, known
limitations, and required on-site gates. It explicitly does **not** claim physical-device acceptance.

## 16. On-site acceptance pack
`docs/go-live/on-site-acceptance/` (8 files): README, environment-sheet, hardware-checklist,
tablet-checklist, service-loop-checklist, financial-checklist, failure-recovery-checklist, signoff.
Every row carries operator/date/tag/device/expected/actual/verdict/evidence/defect/comments. **Nothing is
pre-marked Passed.**

## 17. Hardware result
**Not executed — no physical hardware in the build environment.** Pack prepared for on-site execution.
Not claimed as Passed. (Code path: hardware jobs queue through the outbox and dispatch idempotently.)

## 18. Tablet result
**Not executed — a true 1024×768 CSS viewport requires a physical tablet or a dev-tools-verified browser
viewport** (the CI host is hi-DPI; `resize_window` no-ops). Responsive CSS + RTL present; checklist prepared.
Not claimed as Passed.

## 19. Independent-client result
Multi-client concurrency (cashier + KDS + manager + QR) is proven by the automated suite + prior live
2-worker runs (idempotent seat/fire/pay/merge/aggregator). The **on-site five-physical-client** loop is
prepared in the pack; not executed here. Not claimed as Passed.

## 20. Arabic RTL result
RTL is present and screenshot-evidenced in prior in-browser acceptance (docs/product-acceptance,
docs/ui-acceptance). **True on-tablet RTL at 1024×768 is not executed** here; checklist prepared. Not claimed as Passed.

## 21. Service-loop result
The full arrival→assignment→order→fire→KDS→served→QR→approval→partial→card→receipt→release loop plus
fault injection (86, printer down, KDS/tablet reconnect, lost payment, duplicate QR, worker restart) is
proven as automated scenarios. The **on-site** five-client execution is prepared; not executed here.

## 22. Session and financial sign-off
Native POS session open/close reused; close blocks on unresolved payments. On-site end-of-session drawer
+ POS + accounting reconciliation is in `financial-checklist.md` (required difference 0); executed on-site.

## 23. Defects found
- **D-2 (Major, test-infrastructure):** the runtime/HTTP test suite is **not hermetic** — it assumes a
  pre-provisioned POS environment (config/menu/payment methods) that module install and demo do not create,
  so it is 218-green only on the canonical provisioned DB, not on a bare clean install. **Not a runtime code
  defect** (the code reproduces 218-green from the tag on a provisioned DB). Fix: self-provisioning test base.
- **D-1 (Major-if-required, else Minor):** drive-thru routes reachable by an authenticated terminal — not
  hard-blocked. For a CONTROLLED pilot (trusted staff, single branch, client not provisioned) this is an
  operational-mitigation item, not a code blocker. Candidate RC2 patch if API-level blocking is mandated.
- Environment (not RC): half-deleted `enterprise_19e/web_enterprise` in the workspace addons path (worked around).
No new **Critical** or **financial-Major** defects surfaced.

## 24. Defects fixed
No runtime code changed after tagging (any code change would invalidate RC1 → RC2). D-1 and D-2 are
carried as documented follow-ups (both test-only / operational — no runtime code). Report/evidence honesty
items (reconciliation fixture exclusion, drive-thru classification) resolved by documentation, not code.

## 25. Remaining blockers
- **Controlled pilot:** the on-site gates (§17–22) must be executed by the pilot team — real hardware,
  true 1024×768 tablet, five-client loop, financial sign-off. No **runtime code** blocker remains.
- **Test reproducibility (D-2):** a bare-clean-install green run needs the self-provisioning test base
  (test-only follow-up). Does not block the pilot code, which is verified on the provisioned DB.
- **Public launch:** online card payment, broad live load test, complete staff UAT, live service-day
  evidence, and any enabled-but-unverified channel/provider remain unapproved.

## 26. Controlled-pilot verdict
**Engineering freeze: APPROVED and TAGGED** (`mezze-pilot-rc1` → `634d17e`). The RC is immutable and, from
the tag, installs cleanly and reproduces **218-green on the provisioned DB with no uncommitted-file
dependency**; validator 0-FAIL, reconciliation 0, backup/restore 0-loss all hold. **Controlled-pilot GO is
CONDITIONAL** on: (a) executing the on-site acceptance pack (hardware, true tablet, five-client loop,
financial sign-off) — human gates not executable here and **not marked Passed**; and (b) enforcing the
disabled list (esp. drive-thru client not provisioned). The one honesty caveat is D-2 (non-hermetic suite),
a test-infra follow-up that does not affect the running pilot code.

## 27. Public-launch verdict
**NO-GO.** Blocked by design: no proven online card journey; hardware/tablet/staff-UAT/service-day are
on-site; no broad load test. Post-pilot work is limited to pilot defects, release patches, and the
explicitly deferred capabilities.

---

## Explicit confirmations
- **Exact release commit:** `277338b87e4ca497754474d39f81f44ae45d2aa7` (release content); identity commit `634d17ef923b09295b88ec338931a1606f88205a`.
- **Exact release tag:** `mezze-pilot-rc1` (annotated) → `634d17e`.
- **Working tree clean:** yes — only the two by-design exclusions remain untracked (`CLAUDE.md`, `tests/concurrency/`), plus post-tag report files written after tagging.
- **Secrets found:** none (only install-time random-token generation + fake test fixtures).
- **Exact automated test count:** 218 tests.
- **Exact test exit code:** 0 (pre-commit, and tag-code vs canonical DB).
- **Tests ran from the tagged worktree:** yes — tag code (addons_path → worktree) vs canonical DB = 0 failed / 0 error(s) of 218.
- **Clean installation from the tag succeeded:** yes (module loads, 0 fatal errors); a *bare* clean install's FULL SUITE is not green due to the non-hermetic suite (D-2), not code.
- **Production validation 0 FAIL:** yes (1 env-prerequisite warning: company_timezone).
- **Exact genuine financial order count:** 294 (297 payments).
- **Exact unexplained financial difference:** 0.
- **Backup restore 0 row loss:** yes (17 critical tables, exact match).
- **Exact restore time:** 13.35s (backup 1.06s, 7.6M).
- **Receipt printer executed:** NO — no hardware in this environment (pack prepared; on-site).
- **Kitchen printer executed or N/A:** N/A here — on-site (test-if-used or mark N/A with reason).
- **Cash drawer executed or N/A:** NO — on-site.
- **True 1024×768 CSS viewport executed:** NO — requires physical tablet / dev-tools-verified viewport (on-site).
- **Arabic RTL executed on the tablet:** NO on physical tablet (present + screenshot-evidenced in prior browser acceptance; on-site tablet pending).
- **Separate cashier browser executed:** proven via automated multi-worker concurrency; on-site physical client pending.
- **Separate KDS browser executed:** same — proven in concurrency; on-site physical client pending.
- **Customer QR executed on an independent client:** proven in automated/omnichannel scenarios; on-site independent device pending.
- **Partial and mixed payment executed:** yes (automated money-invariant + runtime scenarios).
- **Lost payment response remained idempotent:** yes (double_pay_race + runtime; same-uuid retry → one payment).
- **KDS disconnect/reconnect executed:** proven in automated/outbox recovery scenarios; on-site physical pending.
- **Waiter-tablet disconnect/reconnect executed:** logic proven; on-site physical pending.
- **One worker killed and recovered:** yes (multi-worker replay evidence; queued work delivers once).
- **Session closing reconciled:** logic verified (close blocks on unresolved payments); on-site drawer sign-off pending.
- **Exact unresolved Critical count:** 0.
- **Exact unresolved Major count:** 2 — D-1 (drive-thru not hard-blocked; operational for a controlled pilot) and D-2 (non-hermetic test suite; test-infra). Neither is a runtime/financial defect.
- **Any code changed after tagging:** no runtime code — RC1 remains valid (a code change would require RC2).
- **Exact evidence directory:** `/home/mageed/odoo_work_19/mezze/addons/mezze_bridge/docs/go-live/`.
- **Controlled pilot approved:** **YES, CONDITIONAL** — engineering freeze approved & tagged; on-site acceptance pack (hardware, true tablet, five-client loop, financial sign-off) must be executed and the disabled list enforced before first live service.
- **Unrestricted public launch approved:** **NO.**
