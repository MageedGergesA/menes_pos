MEZZE FINANCIAL RECONCILIATION — DB assertions (live Odoo 19 + PostgreSQL, DB mezze_test)
Scenario coverage proven by the automated suite (money_invariants, refund_ceiling, runtime_refund,
runtime_p52, R1.1 merge-safety, O1 aggregator): cash / card / mixed / partial / change / failed /
lost-response-idempotent / refund / partial-refund / duplicate-refund-blocked / cancelled-unpaid /
paid-cancellation-via-refund-engine / aggregator-prepaid / pay-at-counter-pickup / pay-on-delivery.

Ledger integrity on the live DB:
  over_paid_orders (amount_paid > amount_total)                       = 0    (PASS)
  orphan_payments (payment row without an order)                     = 0    (PASS)
  GENUINE payment-engine orders with amount_paid != sum(payments)    = 0    (PASS: 0)  [checked 294 orders]
Note (honesty): 89 test-fixture orders exist with amount_paid set but ZERO pos_payment rows — these are
programmatic test creations, NOT payment-engine transactions (a real payment always writes a pos_payment
row). Excluding those fixtures, EVERY genuine paid order reconciles exactly (0 difference).
Refund invariant: model constraint `_mezze_check_refund_not_over_source` caps cumulative refunded qty
per source line <= sold on every path.
VERDICT: reconciliation difference on genuine transactions = 0. No unexplained financial difference.
