# S1.1B — Clean Host Certification — STATUS

> **VERDICT: NOT COMPLETE — clean-host gates NOT EXECUTED.** S1.1B requires two clean Ubuntu 22.04
> hosts with root to prove live systemd/nginx/HTTPS/websocket, reboot auto-start, backup/restore,
> upgrade/rollback. This session runs on the dev laptop with **no passwordless sudo, no VM tooling
> (multipass/lxd/vagrant absent; docker needs sudo), and 8.9 GB free (97% disk)** — so Host A / Host B
> cannot be created here. No Host A/B or live-service gate is marked PASS. `mezze-edge-rc1` is NOT created.

## Executed this session (real)
- **Repo verified:** branch `main`, HEAD `4735030`; `1580da2` + `4735030` both in ancestry; RC1/RC2/RC3 unmoved.
- **S1.1 commits PUBLISHED:** `git push origin main` fast-forward `8219906..4735030` (divergence 0→0, no force).
  Clean hosts can now `git clone` + `checkout 4735030` per the runbook.
- **EDGE_CERT_COMMIT frozen = `4735030c4c58d0511c82e3c4a350c977a2ababb5`** (remote `git@github.com:MageedGergesA/menes_pos.git`).
- **Regression on the certification commit:** deployment self-tests **21/21**; Mezze suite recorded separately.
- **Working tree** clean apart from standing exclusions + this S1.1B runbook/evidence prep.

## NOT EXECUTED (require clean Ubuntu hosts + root → run RUNBOOK.md)
Host A clean install · PostgreSQL live · Odoo prod config live · systemd live + reboot auto-start · nginx
live + `nginx -t` · HTTPS live · websocket live · port-exposure/firewall check · db-manager block (live) ·
release-identity (on host) · Edge validator 0-FAIL (on host) · WAN-status live smoke · local op during WAN
loss (live) · support bundle (on host) · backup/restore (on host, RTO) · live upgrade · rollback · logrotate
live · NTP live · Host A reboot acceptance · Host B clean install + validator + reboot · Host A/B comparison.

## Execution path
`RUNBOOK.md` (this folder) maps every gate A–H to exact commands against `4735030`, with pass criteria and
the final `mezze-edge-rc1` tag step gated on all-PASS. Evidence slots: `install-A/ install-B/ systemd/
nginx/ https/ websocket/ upgrade/ validator/ final/` — all currently NOT EXECUTED.

## Release + physical
`mezze-edge-rc1`: **NOT CREATED** (clean-host gates unmet). Physical gates (printer, drawer, tablet, KDS,
WAN 2-hour, staff UAT, live shift) remain **S1.2 / NOT TESTED**. RC1/RC2/RC3 immutable.
