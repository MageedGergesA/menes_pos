# S1.1 — Mezze Edge Engineering Pack — REPORT

> **Verdict: engineering artifacts BUILT and self-tested; on-host gates (clean-VM install, live
> systemd/nginx/HTTPS bring-up) NOT EXECUTED here** — this host has **no passwordless sudo and no VM
> tooling** (multipass/lxd/vagrant absent; Docker excluded as deploy tech). Those are stated preconditions
> for `mezze-edge-rc1` (§32), so **rc1 is NOT tagged.** No physical hardware gate faked.

## What was built (this pass)
Deployment pack `deploy/edge/`: `install.sh` (+`--dry-run`), `upgrade.sh`, `uninstall.sh`, `validate.sh`,
`backup.sh`, `restore.sh`, `support-bundle.sh`, `release-identity.sh`, `lib/common.sh`, `lib/make-cert.sh`
(local-CA HTTPS), templates (`odoo.conf`, `mezze-edge.service`, `nginx.conf`, `logrotate`, `backup.env`),
`tests/selftest.sh`, `README.md`. Production: `models/golive.py` gained an `edge` validator profile
(additive; default `run()`/`report_text()` unchanged so the suite is unaffected).

## Executed here (real)
- **Deployment self-tests: 21/21 PASS** (`selftest-output.txt`) — bash syntax, template rendering (no
  unresolved vars, no `/home/mageed` leak), `systemd-analyze verify` (unit structurally valid), nginx
  template structure, local-CA cert chains to CA (`openssl verify`), secret redaction, `install.sh --dry-run`.
- **Backup + restore (dev host, not clean VM): 0 row loss** — source orders 137 / payments 140 →
  restored 137 / 140 (delta 0). `backup.sh` produced `db.dump` + `manifest.txt` + `SHA256SUMS` + `.complete`
  marker; `restore.sh` verified checksum + guarded `--yes`. (`transaction_timeout` line is a benign
  cross-version pg_restore warning; data intact.)
- **Edge validator profile** — `mezze.golive.validator.run(profile='edge')` implemented; host-only facts
  (nginx/websocket/service-autostart/NTP/printer/drawer/backup-recency) report **NOT TESTED**, never fake PASS.

## NOT EXECUTED here (require a clean VM + root → S1.1 §19/§20 or S1.2)
Clean-VM install · second clean install · live `systemctl` start/boot · live `nginx -t` on the enabled site
+ HTTPS bind + websocket smoke over TLS · upgrade on a real Edge install. No passwordless sudo / no VM here.

## Gate summary
| Gate | Status |
|---|---|
| Real installer exists | ✅ built (`install.sh`, dry-run passes) |
| nginx config exists | ✅ template built (structure verified; live `nginx -t` needs host) |
| HTTPS strategy executable | ✅ `make-cert.sh` builds a local CA + server cert (verified) |
| systemd service exists | ✅ unit built (`systemd-analyze verify` clean modulo host paths) |
| Release identity works | ✅ `release-identity.sh` (git+odoo+pg+module version) |
| Edge validator exists | ✅ `profile='edge'` |
| Backup script | ✅ built + executed (0 loss) |
| Restore script | ✅ built + executed (0 loss) |
| Backup/restore in a VM (§14) | ⚠ executed on **dev host**, not a clean VM (0 loss) |
| Support bundle + redaction | ✅ built; redaction self-tested |
| WAN status backend/UI (§16/§17) | ❌ **NOT built this pass** (see limitation below) |
| Clean VM install (§19) | **NOT EXECUTED** (no VM/sudo) |
| Second clean install (§20) | **NOT EXECUTED** |
| Upgrade (§21/§22) | ⚠ script built; live upgrade NOT EXECUTED on Edge |
| Rollback path | ✅ backup/restore proven = the rollback path |
| Log rotation | ✅ template built (live rotation needs host) |
| Disk / clock checks | ✅ in the edge validator profile |
| DB not externally exposed | ✅ config binds PG local + odoo http_interface 127.0.0.1 + nginx denies db manager |
| DB manager secured | ✅ `list_db=False` + nginx `deny` on `/web/database/*` |
| Firewall requirements | ✅ documented (only 443 in; PG/Odoo not exposed) |
| Full Mezze automated suite | ✅ see S1.1-report / regression (recorded separately) |
| `mezze-edge-rc1` | **NOT tagged** — clean-VM preconditions unmet here |

## Honest limitation — WAN status backend/UI (§16–18)
Deferred this pass. It is production controller + app-shell (static) code; building it without being able to
run the live stack (no sudo/nginx bring-up) risks regressing the 229-green module with no way to certify the
integration here. It remains designed in `PRODUCT.md` (§8 three-state model) and should be built + tested
when the live Edge stack is available, folded into `mezze-edge-rc1`. Not marked done.

## Release impact
`golive.py` changed (additive edge profile) — RC3 pilot tag stays immutable; this is pre-`mezze-edge-rc1`
engineering. RC1/RC2/RC3 unchanged. No `mezze-edge-rc1` created (preconditions unmet).
